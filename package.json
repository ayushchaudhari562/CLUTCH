{
  "name": "clutch-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "autocommit": "npx nodemon --watch . --ext js,jsx,ts,json,prisma,html,css --exec \"git add . && git commit -m 'Auto-commit' || exit 0\""
  }
}
